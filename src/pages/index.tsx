import Layout from "@/components/layout";

const Homepage = () => {
  return (
    <Layout>
      <div className="flex flex-col">
        <span>Homepage</span>
      </div>
    </Layout>
  );
};

export default Homepage;
